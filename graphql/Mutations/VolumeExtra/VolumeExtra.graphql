
    createVolumeExtra(input: CreateVolumeExtraInput! @spread): VolumeExtra @guard(with: ["api"]) @field(resolver: "App\\GraphQL\\Mutations\\VolumeExtra\\CreateVolumeExtra@resolveVolumeExtra") 
    updateVolumeExtra(id: ID!, input: UpdateVolumeExtraInput! @spread): VolumeExtra @guard(with: ["api"]) @field(resolver: "App\\GraphQL\\Mutations\\VolumeExtra\\UpdateVolumeExtra@resolveVolumeExtra") 
    deleteVolumeExtra(id: ID!): VolumeExtra @guard(with: ["api"]) @field(resolver: "App\\GraphQL\\Mutations\\VolumeExtra\\DeleteVolumeExtra@resolveVolumeExtra") 
