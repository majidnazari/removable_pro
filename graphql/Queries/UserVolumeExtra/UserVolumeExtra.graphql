
getUserVolumeExtra(id: ID!): UserVolumeExtra  @guard(with:["api"]) @field(resolver: "App\\GraphQL\\Queries\\UserVolumeExtra\\GetUserVolumeExtra@resolveUserVolumeExtra") 
    getUserVolumeExtras(
        first: Int, 
        page: Int, 
        # filter: UserVolumeExtraFilterInput
    ): [UserVolumeExtra!]! @guard(with:["api"]) @paginate(builder: "App\\GraphQL\\Queries\\UserVolumeExtra\\GetUserVolumeExtras@resolveUserVolumeExtra")
