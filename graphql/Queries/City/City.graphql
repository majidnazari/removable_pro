
getCity(id: ID!): City @field(resolver: "App\\GraphQL\\Queries\\City\\GetCity@resolveCity") 
    getCities(
        first: Int, 
        page: Int, 
        # filter: CityFilterInput
    ): [City!]! @paginate(builder: "App\\GraphQL\\Queries\\City\\GetCities@resolveCity")
