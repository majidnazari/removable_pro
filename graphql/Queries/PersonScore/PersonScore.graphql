
getPersonScore(id: ID!): PersonScore @guard(with:["api"]) @field(resolver: "App\\GraphQL\\Queries\\PersonScore\\GetPersonScore@resolvePersonScore") 
    getPersonScores(
        first: Int, 
        page: Int, 
        # filter: PersonScoreFilterInput
    ): [PersonScore!]! @guard(with:["api"]) @paginate(builder: "App\\GraphQL\\Queries\\PersonScore\\GetPersonScores@resolvePersonScore")
