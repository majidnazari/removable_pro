
getPersonScore(id: ID!): PersonScore @guard(with:["api"]) @throttle(max: 10, decay: 1) @field(resolver: "App\\GraphQL\\Queries\\PersonScore\\GetPersonScore@resolvePersonScore") 
    getPersonScores(
        first: Int, 
        page: Int, 
        creator_id: Int,
        editor_id: Int,
        person_id: Int,
        score_id: Int,

        score_level: Int,
        status: Status,
        orderBy:[OrderByClause!] @orderBy
        # filter: PersonScoreFilterInput
    ): [PersonScore!]! @guard(with:["api"]) @throttle(max: 10, decay: 1) @paginate(builder: "App\\GraphQL\\Queries\\PersonScore\\GetPersonScores@resolvePersonScore")
