
getMemory(id: ID!): Memory @field(resolver: "App\\GraphQL\\Queries\\Memory\\GetMemory@resolveMemory") 
    getMemories(
        first: Int, 
        page: Int, 
        # filter: MemoryFilterInput
    ): [Memory!]! @paginate(builder: "App\\GraphQL\\Queries\\Memory\\GetMemories@resolveMemory")
