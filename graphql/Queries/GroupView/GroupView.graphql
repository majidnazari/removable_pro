
getGroupView(id: ID!): GroupView  @guard(with:["api"]) @field(resolver: "App\\GraphQL\\Queries\\GroupView\\GetGroupView@resolveGroupView") 
    getGroupViews(
        first: Int, 
        page: Int, 
       
        title: String,
            
        status: Status,
        orderBy:[OrderByClause!] @orderBy
        # filter: GroupViewFilterInput
    ): [GroupView!]! @guard(with:["api"]) @paginate(builder: "App\\GraphQL\\Queries\\GroupView\\GetGroupViews@resolveGroupView")
