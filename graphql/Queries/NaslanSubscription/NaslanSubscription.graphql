
getNaslanSubscription(id: ID!): NaslanSubscription @guard(with:["api"]) @field(resolver: "App\\GraphQL\\Queries\\NaslanSubscription\\GetNaslanSubscription@resolveNaslanSubscription") 
    getNaslanSubscriptions(
        first: Int, 
        page: Int, 
        # filter: NaslanSubscriptionFilterInput
    ): [NaslanSubscription!]! @guard(with:["api"]) @paginate(builder: "App\\GraphQL\\Queries\\NaslanSubscription\\GetNaslanSubscriptions@resolveNaslanSubscription")
