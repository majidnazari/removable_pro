
getQuestion(id: ID!): Question @field(resolver: "App\\GraphQL\\Queries\\Question\\GetQuestion@resolveQuestion") 
    getQuestions(
        first: Int, 
        page: Int, 
        # filter: QuestionFilterInput
    ): [Question!]! @paginate(builder: "App\\GraphQL\\Queries\\Question\\GetQuestions@resolveQuestion")
