
getScore(id: ID!): Score @guard(with:["api"]) @field(resolver: "App\\GraphQL\\Queries\\Score\\GetScore@resolveScore") 
    getScores(
        first: Int, 
        page: Int, 
        title: String,
        status: Status,
       
        orderBy:[OrderByClause!] @orderBy
        # filter: ScoreFilterInput
    ): [Score!]! @guard(with:["api"]) @paginate(builder: "App\\GraphQL\\Queries\\Score\\GetScores@resolveScore")
