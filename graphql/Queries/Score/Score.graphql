
getScore(id: ID!): Score @guard(with:["api"])  @throttle(max: 10, decay: 1)@field(resolver: "App\\GraphQL\\Queries\\Score\\GetScore@resolveScore") 
    getScores(
        first: Int, 
        page: Int, 
        title: String,
        status: Status,
       
        orderBy:[OrderByClause!] @orderBy
        # filter: ScoreFilterInput
    ): [Score!]! @guard(with:["api"]) @throttle(max: 10, decay: 1) @paginate(builder: "App\\GraphQL\\Queries\\Score\\GetScores@resolveScore")
